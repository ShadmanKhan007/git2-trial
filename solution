What is the difference between git restore filename and git restore --staged filename?
 the git restore filename is the name of a file (or multiple files) you want to  restore.
 
 The staged --filename remove the file from the Staging Area, but leaves its act ual modifications untouched.
